---
title: Test-Time Self-Adaptive Conditioning for Stable Audio-Driven Talking-Head Generation
title_zh: 面向稳定音频驱动说话头生成的测试时自适应条件化
authors: "Zhicheng Zhang, Lei Wang, Yu Zhang, Yongsheng Gao"
date: 2026-05-25
pdf: "https://arxiv.org/pdf/2605.25488v1"
tags: ["query:talking-head"]
score: 9.0
evidence: 提出TT-SAC在测试时稳定音频驱动说话头生成
tldr: 音频驱动说话人视频生成通常依赖静态参考图像作为条件，导致身份漂移和时间不一致。TT-SAC 提出一种无参数推理框架，通过将生成器输出反馈给编码器以动态优化条件表示，实现自洽稳定。实验表明，该方法在多种先进模型和基准上显著提升唇同步、时序连贯性和身份保真度，且无需重训或额外监督，为生成式视频模型提供了一种模型无关、训练免费的增强策略。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.25488v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 928, \"height\": 891, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.25488v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1864, \"height\": 583, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.25488v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 692, \"height\": 545, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.25488v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 895, \"height\": 549, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.25488v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 888, \"height\": 541, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.25488v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 696, \"height\": 546, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.25488v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 900, \"height\": 544, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.25488v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 890, \"height\": 543, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.25488v1/fig-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 1856, \"height\": 366, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.25488v1/fig-010.webp\", \"caption\": \"\", \"page\": 0, \"index\": 10, \"width\": 1867, \"height\": 1768, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.25488v1/fig-011.webp\", \"caption\": \"\", \"page\": 0, \"index\": 11, \"width\": 271, \"height\": 346, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.25488v1/fig-012.webp\", \"caption\": \"\", \"page\": 0, \"index\": 12, \"width\": 274, \"height\": 343, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.25488v1/fig-013.webp\", \"caption\": \"\", \"page\": 0, \"index\": 13, \"width\": 274, \"height\": 332, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.25488v1/fig-014.webp\", \"caption\": \"\", \"page\": 0, \"index\": 14, \"width\": 276, \"height\": 336, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.25488v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1872, \"height\": 1611, \"label\": \"Table\"}]"
motivation: 静态单张参考图像无法适应说话时面部动态变化，造成身份特征漂移和时序不连贯，制约生成质量。
method: 推理时构建反馈回路，将生成器当前帧输出重新编码为优化后的条件表达，单步逼近自洽平衡，无需梯度更新。
result: 在主流说话人生成模型和数据集上，唇同步准确率、时序一致性、身份保持和感知质量均获一致提升。
conclusion: TT-SAC 作为模型无关、无训练的测试时自适条件策略，通过条件自适应稳定生成过程，为音频驱动动画提供有效增强机制。
---

## 摘要
音频驱动的说话头生成已借助AniTalker、FLOAT和Sonic等近期模型取得了显著进展。尽管成果斐然，现有方法大多在推理阶段依赖单一静态参考图像来条件化整个视频生成过程。这种静态条件化范式常导致固定的身份特征与动态演变的面部运动之间产生错配，引发身份漂移、时序不一致和感知质量退化。我们提出测试时自适应条件化（TT-SAC），一种无参数的推理框架，使预训练的说话头生成器能在推理过程中自适应其条件化表征，无需重新训练、梯度更新或额外监督。TT-SAC不将参考肖像视为一成不变，而是将生成器与其编码器组合在一个反馈回路中：生成器自身的输出被重新编码，以构建一个能更好匹配合成序列时序动态的精细化条件化表征。单次适应步骤逼近生成过程的自洽平衡，从而稳定身份与运动在时间维度上的表现。我们进一步提供理论分析，表明在温和的Lipschitz假设下，测试时条件适应能降低特征方差并提升生成稳定性，同时呈现一个有原则的偏差-方差权衡，该权衡支配着适应的最优强度。在顶尖说话头生成器和基准数据集上的广泛实验一致表明，TT-SAC在唇同步精度、时序连贯性、身份保持和感知保真度上均有提升。TT-SAC提供了一种模型无关且无需训练的策略来增强生成式视频模型，确立了测试时条件适应作为稳定音频驱动肖像动画的有效机制。

## Abstract
Audio-driven talking-head generation has achieved remarkable progress with recent models such as AniTalker, FLOAT, and Sonic. Despite their success, most existing approaches rely on a single static reference image to condition the entire video generation process at inference stage. This static conditioning paradigm often creates a mismatch between fixed identity features and dynamically evolving facial motion, leading to identity drift, temporal inconsistency, and degraded perceptual quality. We introduce Test-Time Self-Adaptive Conditioning (TT-SAC), a parameter-free inference framework that enables pretrained talking-head generators to adapt their conditioning representations during inference without retraining, gradient updates, or additional supervision. Instead of treating the reference portrait as immutable, TT-SAC composes the generator with its encoder in a feedback loop: the generator's own outputs are re-encoded to construct a refined conditioning representation that better aligns with the temporal dynamics of the synthesized sequence. A single adaptation step approximates a self-consistent equilibrium of the generative process, stabilizing identity and motion across time. We further provide theoretical analysis showing that test-time conditioning adaptation reduces feature variance and improves generative stability under mild Lipschitz assumptions, while exhibiting a principled bias-variance tradeoff that governs the optimal strength of adaptation. Extensive experiments on state-of-the-art talking-head generators and benchmark datasets demonstrate consistent improvements in lip-sync accuracy, temporal coherence, identity preservation, and perceptual fidelity. TT-SAC offers a model-agnostic and training-free strategy for enhancing generative video models, establishing test-time conditioning adaptation as an effective mechanism for stabilizing audio-driven portrait animation.

---

## 论文详细总结（自动生成）

## 1. 研究动机与核心问题

- **研究背景**：音频驱动的说话头生成已取得长足进步，主流方法（如SadTalker、AniTalker、FLOAT、Sonic等）在推理时通常使用**单一静态参考图像**提取身份特征，并以此为条件生成整个视频序列。
- **核心问题**：静态的条件化表征（身份嵌入）与生成过程中**动态演变的面部运动**之间出现错配（mismatch）。随着视频序列推进，固定的身份特征难以适应变化的姿态、表情与微运动，导致：
  - 身份漂移（identity drift）
  - 时序不一致（temporal inconsistency）
  - 局部伪影与感知质量退化
- **研究动机**：能否在**推理阶段**让预训练生成器**自适应地调整条件化表征**，而无需重新训练、梯度更新或修改模型结构？本文由此提出测试时自适应条件化（TT-SAC）框架。

## 2. 方法论

### 2.1 核心思想
- **生成器‑编码器反馈回路**：不再将参考图像的条件视为静态不变，而是利用生成器自身的输出（已生成的帧）重新输入编码器，提取更贴合当前运动动态的身份特征，并以此**精细化条件化表征**。
- **自洽性原理**：期望找到一个身份特征 \(f^*\)，使其满足固定点方程：
  \[
  f^* = \mathbb{E}_t[(E \circ G)(f^*, A)_t]
  \]
  即该特征生成并重新编码后，其平均特征与自身一致，从而达到生成‑编码的自洽平衡。
- **单步适应**：仅执行**一次**蒙特卡洛估计和条件更新，无需多步迭代，避免过度平滑和运动偏差累积。

### 2.2 关键技术与流程
- **条件化算子**：定义 \(T(f) = \mathbb{E}_t[(E \circ G)(f, A)_t]\)，将 TT-SAC 转化为寻求固定点 \(f^* = T(f^*)\)。
- **蒙特卡洛近似**：
  1. 用原始参考特征 \(f_r\) 生成一段初始视频；
  2. 选取前 \(K\) 帧，重新编码得到特征 \(\{f_t\}_{t=1}^K\)；
  3. 计算聚合特征 \(\bar{f} = \frac{1}{K}\sum_{t=1}^K f_t\)；
  4. 用 \(\bar{f}\) 替换原条件特征，进行一次**条件更新**（\(f_r \leftarrow \bar{f}\)）；
  5. 使用更新后的条件特征重新生成最终视频。
- **多流扩展**：除身份（外观）流外，若生成器支持，还可同时对运动流（如姿态关键点、运动场）执行同样的聚合操作，形成 `+ TT-SAC (w/ motion)` 变体。
- **理论支撑**（非必须，但提供 insight）：
  - 聚合降方差：\(\text{Cov}(\bar{f}) = \frac{1}{K}[\Gamma_0 + 2\sum_{\tau=1}^{K-1}(1-\frac{\tau}{K})\Gamma_\tau]\)。
  - 输出方差被 Lipschitz 常数 \(L_G\) 和 \(\bar{f}\) 的协方差所约束（Lemma 1）。
  - 单步蒙特卡洛迭代具有期望意义上的收缩性质，向固定点靠近（Lemma 2）。
  - 偏差‑方差分解（Proposition 3）：\(\mathbb{E}\|G(\bar{f})-G(\mu)\|^2 \approx \text{bias}^2 + \text{variance}\)，解释 \(K\) 值的选择应平衡因时间非平稳性引入的偏差和采样带来的方差。

### 2.3 伪代码级流程（文字描述）
1. 输入：参考图像 \(I_r\)，音频 \(A\)，预训练编码器 \(E\) 和生成器 \(G\)。
2. 第一步生成：\( \{\hat{I}_t^{(\text{init})}\} = G(E(I_r), A) \)。
3. 选取前 \(K\) 帧，提取特征：\( f_t = E(\hat{I}_t^{(\text{init})}) \)。
4. 聚合：\( \bar{f} = \frac{1}{K}\sum_{t=1}^K f_t \)。
5. 条件更新：\( f_{\text{new}} \leftarrow \bar{f} \)。
6. 最终生成：\( \{\hat{I}_t^{(\text{final})}\} = G(f_{\text{new}}, A) \)。

## 3. 实验设计

### 3.1 数据集与场景
- **Hallo**：100个4秒片段。
- **RAVDESS**：50个4～10秒片段，包含24个不同身份。
- **CelebV-HQ**：50个4秒片段。
- 所有视频帧率25 fps，音频16 kHz，人脸裁剪并resize至512×512。

### 3.2 对比方法与基准
- **基准模型**（5种预训练生成器）：SadTalker、Sonic、AniTalker、FLOAT、JoyVASA。
- **对比设置**：
  - Baseline（各生成器原始推理，无TT-SAC）。
  - `+ TT-SAC`（仅身份流上的条件自适应）。
  - `+ TT-SAC (w/ motion)`（同时自适应身份流与运动流，若模型支持）。

### 3.3 评估指标
- **唇音同步**：Sync‑C (↑) 和 Sync‑D (↓)。
- **时序平滑度**：Smooth (↑)。
- **感知质量**：LPIPS (↓)。
- **身份保持**：余弦相似度 CSIM (↑)。
- **视频级保真度**：FID (↓) 和 FVD (↓)。

## 4. 资源与算力
- 本文未提及任何GPU型号、数量或训练时长。**TT-SAC 是一种纯推理时的策略，无需训练或梯度更新**，因此不涉及大规模额外算力开销。其额外计算主要来自一次额外的生成‑编码‑聚合过程，远小于重新训练或微调。

## 5. 实验充分性与数量
- **主实验**：在5个主流生成器 × 3个数据集上，对比 Baseline 与两种 TT-SAC 变体的全部7项指标，形成多维度评测矩阵（详见 Table I）。
- **消融分析**：考察聚合帧数 \(K\) 的影响（\(K=0\) 到 10），覆盖 Sync‑C、Sync‑D、CSIM、FID 的变化趋势（Figure 4），验证偏差‑方差权衡。
- **运动流消融**：比较仅身份流 TT-SAC 与同时包含运动流 TT-SAC 的效果差异。
- **跨任务泛化**：在 OmniAvatar 上测试半身人像与虚拟角色动画，展示方法在其他音频驱动视频任务上的适用性（Figure 5）。
- **定性分析**：展示多组典型失败案例（眼变形、身份漂移、面部突变等）的对比结果（Figure 3）。

总体而言，实验设计充分、对照严格，涵盖定量与定性、多模型多数据集、消融与泛化，评估较为全面且具有公平性。

## 6. 主要结论与发现
1. **一致改善**：TT-SAC 在所有测试的生成器与数据集上，均能提升唇音同步、时序连贯性、身份保持与感知质量，并降低 FID 和 FVD。
2. **最优聚合帧数 \(K\)**：较小的 \(K\)（如 1 或 2）往往带来最大增益；继续增大 \(K\) 会导致收益饱和甚至轻微下降，与理论中的偏差‑方差权衡相呼应。
3. **运动流增强**：在支持运动流自适应的模型上，进一步改进面部结构稳定性和时间一致性，但在某些指标上可能与身份流形成微小权衡。
4. **泛化能力**：TT-SAC 同样适用于非脸部视频生成任务（如半身或虚拟角色），证实其模型无关和任务无关的特性。
5. **理论解释**：从方差降低、局部压缩和偏差‑方差分解角度给出了 TT-SAC 有效性的原则性解释。

## 7. 方法亮点与优势
- **训练免费、模型无关**：无需微调，可直接插拔应用于任何具有编码器‑条件生成器结构的预训练说话头模型。
- **根源问题导向**：直指静态条件与动态生成之间的失配，从条件层面进行自适应，而非简单后处理或修改生成过程。
- **单步自洽迭代**：仅一次反馈循环，计算额外开销小，同时避免多步迭代可能引入的漂移或过度平滑。
- **理论完备**：通过固定点迭代、协方差分析和偏差‑方差分解，为方法提供形式化解释，增强可信度。
- **多模态扩展**：可灵活应用于身份流和运动流，展示出良好的拓展性。
- **跨任务潜力**：在非标准说话头任务（如半身、虚拟角色）上也有效，方法边界较宽。

## 8. 不足与局限
- **初始生成质量依赖**：若基线生成器在早期帧已产生严重身份退化或伪影，则聚合特征可能继承偏差，TT-SAC 的改善幅度会受限。
- **聚合帧数 \(K\) 需调节**：\(K\) 的选择影响偏差‑方差平衡，目前按固定启发式或人工选定，缺乏自动化或任务自适应策略。
- **简单平均聚合**：使用平均池化可能忽略不同帧的重要性差异，未来可探索加权聚合或时序注意力等更精细的聚合方式。
- **未探索多步迭代**：实际仅用了一步蒙特卡洛更新；虽然理论分析表明多步可能收敛更好，但可能面临稳定性和计算开销问题，本文未深入研究。
- **仅在特定编码器‑生成器框架下验证**：虽然测试了5个不同范式的模型，但方法的前提是生成器有可用的编码器和清晰的条件注入路径，对黑箱商业模型可能难以直接应用。
- **部分指标上改进微弱**：对已经高度优化的基线（如某些模型在某些数据集），同步类指标的提升相对较小，说明方法带来的增益与基线本身的成熟度有关。

（完）
