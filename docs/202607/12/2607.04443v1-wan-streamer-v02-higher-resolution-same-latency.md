---
title: "Wan-Streamer v0.2: Higher Resolution, Same Latency"
title_zh: "Wan-Streamer v0.2: 更高分辨率，相同延迟"
authors: "Lianghua Huang, Zhi-Fan Wu, Yupeng Shi, Wei Wang, Mengyang Feng, Junjie He, Chen-Wei Xie, Yu Liu, Jingren Zhou, Ang Wang, Bang Zhang, Baole Ai, Chen Liang, Cheng Yu, Chongyang Zhong, Jinwei Qi, Kai Zhu, Pandeng Li, Peng Zhang, Wenyuan Zhang, Xinhua Cheng, Yitong Huang, Yun Zheng, Yuxiang Bao, Yuzheng Wang, Zoubin Bi"
date: 2026-07-05
pdf: "https://arxiv.org/pdf/2607.04443v1"
tags: ["query:talking-head"]
score: 9.0
evidence: 实时音视频交互模型，支持中景智能体面部动画
tldr: 实时视听交互模型Wan-Streamer v0.2在保持约200ms模型端延迟的前提下，将输出分辨率从192x336提升至640x368。采用thinker-performer分离架构：thinker单GPU处理流式感知和语言缓存，performer多GPU通过Ulysses并行对高分辨率潜视频分片去噪，音频则不并行。该设计在不增加用户延迟的情况下支持更丰富的场景细节，总交互延迟约550ms（含网络），为下一代流式交互提供了可行方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.04443v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1699, \"height\": 775, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.04443v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1708, \"height\": 664, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.04443v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1719, \"height\": 658, \"label\": \"Table\"}]"
motivation: 提升流式视听交互模型的输出分辨率以支持实时对话中细粒度代理理解，同时保持低延迟。
method: 保持thinker-performer架构，thinker单GPU低延迟处理感知与语言；performer多GPU Ulysses并行对高分辨率潜视频分片去噪，音频不并行以减少通信。
result: 分辨率从192x336提升至640x368，模型侧延迟仍约200ms，总延迟约550ms（含350ms网络预算）。
conclusion: 通过高效的分片与缓存策略，在不大幅增加延迟的前提下实现了高分辨率流式交互，增强了实时场景细节。
---

## 摘要
我们提出Wan-Streamer v0.2，这是一种保持延迟的升级版原生流式端到端视听交互模型。v0.2保留了v0.1的建模公式，但将交互输出流从192x336提升至640x368，同时在25 FPS下保持约200 ms的模型端信号到信号延迟。更高分辨率的流支持场景锚定的中景智能体，其在实时对话期间，姿态、视线、手部、附近物体和局部场景布局保持清晰可辨。为了支持更大的视觉流而不增加用户可见的延迟，v0.2将思考器保持为单GPU低延迟路径，用于流式感知、构建生成缓存的短语言/状态Transformer传递以及最终解码。执行器则变成一个多GPU的Ulysses风格上下文并行组，用于昂贵的下一个单元潜变量生成。每个执行器将传入的K/V写入预先分片的本地缓存。长的潜变量视频序列在多个执行器之间分割，通过Ulysses通信进行去噪，而更短的音频潜变量序列则在不进行序列分片的情况下生成。在这种分割中，思考器的语言/状态计算仅作为K/V条件传递给执行器，因此无需在执行器组内传递单独的语言序列。这可以将额外的硬件集中在视觉生成上，同时保持紧凑的思考器-执行器边界，在包含350 ms双向网络预算时，总远程交互延迟保持在约550 ms。

## Abstract
We present Wan-Streamer v0.2, a latency-preserving upgrade of the native-streaming, end-to-end audio-visual interaction model. v0.2 keeps the v0.1 modeling formulation, but raises the interactive output stream from 192x336 to 640x368 while preserving approximately 200 ms model-side signal-to-signal latency at 25 FPS. The higher-resolution stream supports scene-grounded mid-shot agents whose posture, gaze, hands, nearby objects, and local scene layout remain legible during real-time conversation. To support the larger visual stream without adding user-visible delay, v0.2 keeps the thinker as a single-GPU low-latency path for streaming perception, the short language/state Transformer pass that builds the generation cache, and final decoding. The performer becomes a multi-GPU Ulysses-style context-parallel group for the expensive next-unit latent generation. Each performer rank writes incoming K/V into a pre-sharded local cache. The long high-resolution latent video sequence is split across ranks for denoising and gathered through Ulysses communication, while the much shorter audio latent sequence is generated without sequence sharding. In this split, the thinker's language/state computation reaches the performer only as K/V conditioning, so no separate language sequence has to be communicated inside the performer group. This concentrates additional hardware on visual generation while preserving the compact thinker-performer boundary, keeping total remote interaction latency at approximately 550 ms when a 350 ms bidirectional network budget is included.

---

## 论文详细总结（自动生成）

# 论文详细总结

## 1. 核心问题与整体含义（研究动机和背景）
- **问题**：在实时音视频交互场景（如数字人对话）中，现有端到端流式模型 Wan-Streamer v0.1 的输出分辨率仅为 192×336，这导致中景（mid-shot）画面下智能体的姿态、视线、手势、附近物体和场景布局等细节无法清晰辨识，限制了场景锚定交互（scene-grounded interaction）的能力。
- **背景**：全双工语音系统、多模态感知、流式视频生成和实时数字人等领域的进展要求模型能够在低延迟（模型端约200ms）下同时处理并生成音频和视频。v0.1 建立了原生流式建模方法，但视觉分辨率成为瓶颈。
- **整体含义**：本文旨在不增加用户感知延迟的前提下，将输出分辨率提升至 640×368，从而支持更丰富的视觉上下文，使智能体在与用户实时对话中保持身体细节可读，拓展交互范围（从特写式通话转向场景锚定的中景对话）。

## 2. 论文提出的方法论：核心思想、关键技术细节、算法流程
### 核心思想
- 保持 v0.1 的“原生流式”公式不变（即用户和智能体的文本、音频、视频在同一因果时间线上，由单个 Transformer 统一建模），通过改变推理部署架构来吸收高分辨率生成的计算开销，从而保持约200ms模型端延迟。

### 关键技术细节
- **Thinker-Performer 分离架构**：
  - **Thinker（思考器）**：位于单 GPU 上，负责流式感知、语言/状态更新、KV 缓存构建以及最终的因果解码。这是一个低延迟的控制路径。
  - **Performer（执行器）**：位于多 GPU 上，采用 Ulysses 风格的上下文并行（context parallelism）组，专门承担高开销的潜变量生成（flow-matching 去噪）。
- **通信与缓存**：
  - Thinker 将紧凑的、与 performer 兼容的 K/V 切片广播给所有 performer 等级（rank）。
  - 每个 performer rank 将收到的 K/V 写入预先分片的本地缓存（pre-sharded local cache）。
  - 高分辨率潜视频序列在多个 performer rank 之间按序列维度分片，通过 Ulysses all-to-all/gather 通信完成注意力计算和去噪。
  - 音频潜变量序列远少于视频令牌，因此不进行序列分片，直接在每个 rank 上完整生成。
- **算法流程（每 160ms 单元）**：
  1. Thinker 消费当前用户观测（编码），执行语言/状态 Transformer 传递，产出新的 performer 兼容 K/V 切片。
  2. Thinker 接收上一单元生成的潜变量，解码并输出响应的音频/视频。
  3. Performer ranks 接收到新 K/V 切片后，更新各自分片的完整历史缓存，然后运行 Ulysses 上下文并行去噪，为下一单元生成高分辨率视频潜变量和音频潜变量。
  4. 整个过程需要适配 160ms 的流式单元时间，以保证模型端延迟约200ms（含编码、状态更新、潜生成、解码）。

## 3. 实验设计：数据集、基准、对比方法
- **数据集**：论文未使用公开 benchmark 数据集。实验基于生成的 640×368 对话视频进行定性观察。
- **基准（Benchmark）**：与自身前代 v0.1 进行比较，重点在分辨率和延迟。延迟测量使用与 v0.1 相同的“响应边界”：从用户 160ms 单元可供 thinker 开始，到对应音视频响应单元解码完成并准备好输出为止。
- **对比方法**：未与其他实时系统（如 StreamAvatar、Doubao Realtime Voice、GPT-4o 等）进行直接对比，仅指出这些系统报告的延迟端点不同（如首包语音延迟、首帧延迟等），故保持自身测量惯例。
- **定性观察**：关注视觉稳定性与可读性，包括面部细节、视线、口型、手部、姿势、附近物体和场景布局。

## 4. 资源与算力
- **文中未明确说明**训练所需的 GPU 型号、数量或训练时长。仅提及推理部署时：
  - Thinker 使用 **单 GPU**（未指定型号）。
  - Performer 使用 **多 GPU**（数量未说明，Ulysses 上下文并行组）。
- 推理延迟数据（约200ms）是在特定硬件条件下测得的，但硬件规格未被列出。

## 5. 实验数量与充分性
- **实验数量**：只有一组延迟时间测量（与 v0.1 对比，结果一致），加上一段定性视觉描述。未进行消融实验、分辨率消融、并行度消融或自动化质量指标（如 PSNR、FID、LPIPS）评估。
- **充分性与公平性**：
  - 延迟测量采用统一边界，与 v0.1 一致，较为客观。
  - 定性观察缺乏量化证据，且未与竞品系统在相同条件下对比，说服力有限。
  - 未评估模型在不同网络条件、不同场景（如多人、复杂背景、快速运动）下的表现。
  - 实验覆盖不足，难以判断方法在所有维度上的有效性。

## 6. 论文的主要结论与发现
- **成功保持延迟**：通过 Thinker-Performer 分离和 Ulysses 上下文并行，v0.2 在提升分辨率至 640×368 的同时，模型端延迟仍保持约 200ms（总交互延迟约 550ms，含 350ms 双向网络预算）。
- **视觉质量提升**：支持更高清晰度的特写通话和场景锚定的中景智能体，使姿态、视线、手势、物体和场景布局在实时对话中清晰可辨。
- **架构有效性**：将昂贵的高分辨率视频生成集中在多 GPU Performer 群组上，Thinker 保持紧凑低延迟路径，通信开销受控（仅广播 K/V 切片，视频分片通信局限于 performer 组内）。
- **扩展交互形式**：从仅限特写帧向“情景化对话”扩展。

## 7. 优点：方法或实验设计上的亮点
- **设计优雅**：Thinker-Performer 分离将计算与延迟关键路径隔离，使分辨率提升不显著影响交互延迟。
- **通信高效**：语言计算在 Thinker 阶段折叠为 K/V 条件，无需在 performer 组内传递语言序列；视频分片利用 Ulysses 通信模式，内存和带宽利用合理。
- **保持统一公式**：仍为单一流式模型，避免了模块级联的复杂性。
- **实际价值**：直接提升了实时数字人交互的视觉体验，有明确应用场景。

## 8. 不足与局限
- **缺乏定量评估**：没有使用任何图像/视频质量指标（如 PSNR、SSIM、FVD、CLIP 评分）或用户调研来客观衡量视觉提升。仅凭作者观察，存在偏倚。
- **实验对比不足**：未与同类实时系统在相同硬件和环境进行公平对比，仅与自身 v0.1 比较，无法体现相对优势。
- **未探讨性能边界**：未报告分辨率更高（如 720p/1080p）或帧率更高（如 30FPS）时的延迟影响，也未分析 performer 数量和 Ulysses 分片大小的 trade-off。
- **未分析网络预算假设的稳健性**：350ms 双向网络预算是一个固定假设，实际环境中网络抖动可能显著影响端到端体验。
- **缺失训练细节**：未说明模型如何训练、数据规模和配方，读者无法复现或评估训练成本。
- **分辨率仍然有限**：640×368 并非高清（720p/1080p），且宽高比特殊（接近 16:9 但非标准），可能限制了与标准显示设备匹配。
- **定性观察的偏差风险**：作者是模型开发者，定性判断可能带有主观倾向。

（完）
