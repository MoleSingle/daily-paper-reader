---
title: "Wan-Streamer v0.2: Higher Resolution, Same Latency"
title_zh: "Wan-Streamer v0.2: 更高分辨率，相同延迟"
authors: "Lianghua Huang, Zhi-Fan Wu, Yupeng Shi, Wei Wang, Mengyang Feng, Junjie He, Chen-Wei Xie, Yu Liu, Jingren Zhou, Ang Wang, Bang Zhang, Baole Ai, Chen Liang, Cheng Yu, Chongyang Zhong, Jinwei Qi, Kai Zhu, Pandeng Li, Peng Zhang, Wenyuan Zhang, Xinhua Cheng, Yitong Huang, Yun Zheng, Yuxiang Bao, Yuzheng Wang, Zoubin Bi"
date: 2026-07-05
pdf: "https://arxiv.org/pdf/2607.04443v3"
tags: ["query:talking-head"]
score: 9.0
evidence: 高分辨率流式谈话头模型
tldr: Wan-Streamer v0.2在保持约200ms模型端延迟的前提下，将交互输出分辨率从192x336提升至640x368，支持场景接地中景智能体的细粒度细节。该方法维持v0.1的建模框架，通过thinker单GPU低延迟路径处理感知与缓存构建，performer采用多GPU Ulysses上下文并行对高分辨率视频潜序列进行分割去噪与通信。语言计算仅作为K/V条件传递至performer，避免组内额外通信，使得总远程交互延迟（含350ms网络预算）约550ms。该工作实现了分辨率提升而不牺牲交互实时性，对高保真远程音视频交互有重要意义。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.04443v3/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1699, \"height\": 775, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.04443v3/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1708, \"height\": 664, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.04443v3/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1719, \"height\": 658, \"label\": \"Table\"}]"
motivation: 现有音视频交互模型分辨率不足，无法在实时对话中清晰展示智能体姿态、手势等细节，需在保持低延迟下提高分辨率。
method: 保持thinker单GPU低延迟路径，performer多GPU Ulysses上下文并行分割去噪高分辨率视频潜序列，语言计算仅作为K/V条件传递。
result: 分辨率从192x336提升至640x368，模型端信号延迟保持约200ms，含网络预算的总交互延迟约550ms。
conclusion: 在不增加用户可见延迟下显著提升分辨率，使实时对话中智能体细节清晰可见，拓展了高保真音视频交互的应用。
---

## 摘要
我们推出了Wan-Streamer v0.2，这是对原生流式端到端音视频交互模型的延迟保持升级。v0.2保留了v0.1的建模框架，但将交互输出流从192x336提升至640x368，同时保持约200毫秒的模型端信号到信号延迟（25 FPS）。更高分辨率的流支持基于场景的中景智能体，其实时对话过程中姿势、视线、手部、附近物体和局部场景布局仍保持清晰可辨。为了在不增加用户可感知延迟的情况下支持更大的视觉流，v0.2将思考者保持为单GPU低延迟路径，用于流式感知、构建生成缓存的短语言/状态Transformer传递，以及最终解码。执行者则变为多GPU的Ulysses风格上下文并行组，用于昂贵的下一单元潜变量生成。每个执行者秩将传入的K/V写入预分片的本地缓存。长高分辨率潜变量视频序列被跨秩分割进行去噪，并通过Ulysses通信聚合，而较短的音频潜变量序列则无需序列分片即可生成。在这种分割中，思考者的语言/状态计算仅作为K/V条件传入执行者，因此无需在执行者组内传递单独的语言序列。这将在保持紧凑的思考者-执行者边界的同时，将额外硬件集中在视觉生成上，使得在包含350毫秒双向网络预算时，总远程交互延迟约为550毫秒。

## Abstract
We present Wan-Streamer v0.2, a latency-preserving upgrade of the native-streaming, end-to-end audio-visual interaction model. v0.2 keeps the v0.1 modeling formulation, but raises the interactive output stream from 192x336 to 640x368 while preserving approximately 200 ms model-side signal-to-signal latency at 25 FPS. The higher-resolution stream supports scene-grounded mid-shot agents whose posture, gaze, hands, nearby objects, and local scene layout remain legible during real-time conversation. To support the larger visual stream without adding user-visible delay, v0.2 keeps the thinker as a single-GPU low-latency path for streaming perception, the short language/state Transformer pass that builds the generation cache, and final decoding. The performer becomes a multi-GPU Ulysses-style context-parallel group for the expensive next-unit latent generation. Each performer rank writes incoming K/V into a pre-sharded local cache. The long high-resolution latent video sequence is split across ranks for denoising and gathered through Ulysses communication, while the much shorter audio latent sequence is generated without sequence sharding. In this split, the thinker's language/state computation reaches the performer only as K/V conditioning, so no separate language sequence has to be communicated inside the performer group. This concentrates additional hardware on visual generation while preserving the compact thinker-performer boundary, keeping total remote interaction latency at approximately 550 ms when a 350 ms bidirectional network budget is included.

---

## 论文详细总结（自动生成）

# Wan-Streamer v0.2: 更高分辨率，相同延迟 – 论文结构化总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：实时音视频交互模型（如Wan-Streamer v0.1）的输出分辨率仅为192×336，导致在对话中无法清晰呈现智能体的身体姿势手势、附近物体及场景布局，限制了“场景接地中景交互”的应用。
- **背景**：全双工口语对话、多模态感知、流式视频生成和数字人技术的发展，需要端到端、低延迟的音频-视频交互系统。v0.1已实现原生流式框架，但分辨率不足，无法支持更丰富的视觉沟通。
- **研究动机**：在不增加模型端信号延迟（约200ms）的前提下，将输出分辨率提升至640×368，使实时对话中智能体的细节清晰可辨，从而拓展应用场景（如视频通话、情景化对话）。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **总体思路**：保持v0.1的原生流式建模框架（统一因果时间线、块因果注意力），但改变部署拓扑，将高分辨率的视觉生成计算分离到多GPU，同时保留低速延迟控制路径。
- **核心思想**：将模型拆分为两个角色 —— **Thinker（思考者）** 和 **Performer（执行者）**：
  - **Thinker**：单GPU，负责流式感知（编码）、语言/状态Transformer更新、K/V缓存构建、最终解码。该路径保持低延迟（约200ms）。
  - **Performer**：多GPU的Ulysses风格上下文并行组，处理高分辨率潜视频的流匹配生成。
- **关键技术细节**：
  - **K/V通信**：Thinker生成紧凑的、Performer兼容的K/V切片并广播给所有Performer rank。每个rank将切片写入预分片的本地缓存中。
  - **序列并行**：高分辨率视频潜变量序列在Performer组内按序列维度分割（Ulysses all-to-all/gather通信），用于去噪注意力的计算。音频潜变量序列较短，不进行分片。
  - **语言计算分离**：Thinker的语言/状态计算只有K/V条件传递到Performer，无需在Performer组内传递完整语言序列，降低了通信开销。
  - **延迟控制**：整个流水线（Thinker感知+状态更新+Performer潜生成+Thinker解码）在160ms流式单元内完成，保持模型端约200ms，总交互延迟（含350ms双向网络预算）约550ms。

## 3. 实验设计：使用的数据集/场景、Benchmark、对比方法

- **数据集/场景**：论文未明确指定公开数据集。实验基于生成的640×368对话视频进行 **定性观察**，关注输出在聆听和说话期间的视觉稳定性、清晰度（面部细节、视线、嘴部运动、手势等）。
- **Benchmark**：没有使用标准图像/视频生成质量指标（如FID, LPIPS, PSNR等）。主要比较对象是 **Wan-Streamer v0.1** 的延迟和分辨率。
- **对比方法**：未与其他实时音视频模型（如StreamAvatar, GPT-4o, Doubao等）进行定量对比。仅引用了相关系统，但无实验比较。
- **测量协议**：模型端信号到信号延迟从用户160ms单元到达Thinker，到对应音频-视频单元解码完毕为止。采用与v0.1相同的边界，以确保一致性。

## 4. 资源与算力

- **未明确说明**：论文未提及训练或推理所用的具体GPU型号（如A100、H100）、数量、训练时长或显存消耗。
- **已知信息**：Thinker使用单GPU，Performer使用多GPU（Ulysses上下文并行组），但具体组大小未给定。音频生成不进行序列分片，但未给出所需计算资源量。

## 5. 实验数量与充分性

- **实验数量**：仅包含一组延迟测量（结果：模型端~200ms，总~550ms）及定性视觉观察。没有：
  - 消融实验（如不同分辨率、不同并行策略、不同组大小的影响）
  - 定量质量评估（分辨率提升对生成质量数值指标的影响）
  - 与竞争方法（如StreamAvatar、MiniCPM-o 4.5等）的对比实验
  - 用户研究或A/B测试
- **充分性评估**：**不充分**。实验仅证明延迟目标可达且输出视觉清晰，但缺乏普遍性验证和对质量的一致度量。结论的可靠性更多依赖于架构推理而非系统化实验。

## 6. 论文的主要结论与发现

- **主要结论**：Wan-Streamer v0.2在不增加模型端延迟（~200ms）的前提下，成功将交互输出分辨率从192×336提升至640×368，总远程交互延迟仍保持在550ms（含350ms网络预算）。
- **发现**：通过Thinker-Performer解耦（Thinker单GPU低延迟，Performer多GPU上下文并行），可将高分辨率视频生成的计算负担从延迟关键路径移除，同时保持交互实时性。
- **应用价值**：支持更高保真的视频通话交互，并首次允许在实时对话中呈现场景接地中景智能体的细节（身体姿态、手势、物体、场景布局）。

## 7. 优点：方法或实验设计上的亮点

- **方法创新**：提出了面向实时流式交互的Thinker-Performer分离架构，通过预分片K/V缓存和Ulysses序列并行，在不牺牲延迟的情况下大幅提升视觉分辨率。
- **延迟保持**：创新性地将昂贵的视觉生成计算并行化，而不对输入感知和输出解码增加延迟，保持了与v0.1相同的低延迟。
- **通信高效**：语言计算仅传递K/V条件，避免在Performer组内传输长语言序列，减少了跨GPU通信开销。
- **实用导向**：直接面向产品级场景（视频通话、虚拟数字人），且明确指出网络预算（350ms双向）和总交互延迟（550ms），便于系统集成。

## 8. 不足与局限

- **缺乏量化评估**：未提供任何生成质量量化指标（如FID、LPIPS、MOS、用户评分），仅依靠定性观察，难以客观衡量分辨率提升对真实可用性的影响。
- **无竞争对比**：未与其他近期实时音视频交互系统（如MiniCPM-o 4.5、StreamAvatar、Hallo-Live等）进行公平比较，无法说明本方法的相对优势。
- **消融实验缺失**：未对并行策略（Performer组大小）、K/V分片粒度、延迟分配等进行消融，结论的鲁棒性不明确。
- **硬件资源缺乏透明度**：未指定GPU型号、数量及训练/推理成本，可复现性受限。
- **应用限制**：需要专用多GPU集群部署，单GPU无法运行；对网络带宽有严格要求（350ms双向）；仅支持640×368一种分辨率，泛化能力未验证。
- **偏差风险**：实验仅基于内部分布数据，可能存在数据偏差；定性观察主观性强，结论可能不具普遍性。

（完）
