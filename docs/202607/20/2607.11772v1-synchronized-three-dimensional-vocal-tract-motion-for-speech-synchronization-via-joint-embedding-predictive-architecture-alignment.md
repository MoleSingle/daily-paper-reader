---
title: Synchronized Three-Dimensional Vocal-Tract Motion for Speech Synchronization via Joint-Embedding Predictive Architecture Alignment
title_zh: 基于联合嵌入预测架构对齐的语音同步三维声道运动同步
authors: "Sheng Li, Takahiro Shinozaki"
date: 2026-07-13
pdf: "https://arxiv.org/pdf/2607.11772v1"
tags: ["query:talking-head"]
score: 9.0
evidence: 语音同步的三维声道运动
tldr: "现代神经语音系统虽能合成高质量波形，但隐藏了发声物理状态；生物力学声道模型虽提供发音结构，但合成音频不够鲁棒。本文提出通过联合嵌入预测架构（JEPA）表示和强化学习/交叉熵方法（RL/CEM）轨迹选择，将时长保持的声学载体与修正的三维声道模型对齐，生成同步的下颌、舌、唇等运动。在24词测试集上，自动语音识别词错误率8.33%、字符错误率4.17%，语音自然度UTMOS 3.174，JEPA评分0.864，音色守卫评分0.947。该方法实现了可解释、物理合理的同步发音运动生成，为语音交互提供了可视化发音状态。"
source: arxiv
selection_source: fresh_fetch
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.11772v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 862, \"height\": 342, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.11772v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 826, \"height\": 278, \"label\": \"Table\"}]"
motivation: 现有神经语音系统缺乏可解释的发音运动，而生物力学模型合成音频不鲁棒，亟需一种能同步生成物理一致三维声道运动的方法。
method: 采用时长保持声学载体作为波形，结合修正的三维声道模型，通过JEPA风格表示和RL/CEM轨迹选择循环对齐发音动作与声学特征及物理约束。
result: "在24词集上，ASR词错误率8.33%、字符错误率4.17%，UTMOS 3.174，JEPA评分0.864，音色守卫评分0.947。"
conclusion: 该方法能生成与声学载体同步且物理合理的三维声道运动，显著提升了语音生成系统的可解释性和可视化能力。
---

## 摘要
现代神经语音系统可以生成可理解波形，但通常隐藏产生声音的物理语音产生状态。相反，生物力学声道模型揭示了发音结构、接触行为、气流路径和几何约束，但直接物理波形合成仍不如现代神经声码器稳健。一种持续时间保持的声学载体提供听觉波形，而校正后的三维声道模型提供同步的下颌、唇、舌、软腭、喉、口腔气流和鼻腔气流运动。联合嵌入预测架构（JEPA）风格的表示和强化学习/交叉熵方法（RL/CEM）轨迹选择循环将发音动作与声学载体和物理合理性约束对齐。评估包含12个三维录音，覆盖24个最小对刺激。在24词集合上，载体获得了良好的自动语音识别（ASR）结果（词错误率8.33%，字符错误率4.17%），UTMOS得分为3.174，平均JEPA得分为0.864，平均音色保护得分为0.947。

## Abstract
Modern neural speech systems can generate intelligible waveforms, but they usually hide the physical speech-production state that produced the sound. Conversely, biomechanical vocal-tract models expose articulatory structure, contact behavior, airflow routing, and geometric constraints, but direct physical waveform synthesis remains less robust than modern neural vocoders. A duration-preserving acoustic carrier supplies the listening waveform, while a corrected three-dimensional vocal-tract model supplies synchronized jaw, lip, tongue, velum, laryngeal, oral-airflow, and nasal-airflow motion. A joint-embedding predictive architecture (JEPA)-style representation and a reinforcement learning/cross-entropy method (RL/CEM) trajectory-selection loop align articulatory actions to the acoustic carrier and to physical-plausibility constraints. The evaluation contains 12 3D recordings covering 24 minimal-pair stimuli. On the 24-word set, the carrier obtains good automatic speech recognition (ASR) results (an 8.33\% WER, a 4.17\% CER), a UTMOS score of 3.174, a mean JEPA score of 0.864, and a mean timbre-guard score of 0.947.

---

## 论文详细总结（自动生成）

好的，作为一名资深学术论文分析助手，我将严格按照您的要求，对提供的论文《Synchronized Three-Dimensional Vocal-Tract Motion for Speech Synchronization via Joint-Embedding Predictive Architecture Alignment》进行结构化、深入、客观的中文总结。

---

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：现代神经语音系统（如 TTS、神经声码器）虽能生成高清晰度的音频波形，但隐藏了产生该声音的物理机制（如声道形状、发音器官运动），缺乏可解释性。另一方面，生物力学声道模型虽能提供详细的发音结构、接触行为、气流路径等可解释信息，但其直接合成的物理波形在音质和鲁棒性上远逊于现代神经声码器。因此，如何将高质量的音频信号与物理上合理、同步且可解释的三维声道运动结合起来，是本文的核心挑战。

- **整体含义**：本文旨在构建一个**混合系统**，该系统**不强求**通过物理模拟直接生成最终语音，而是采用“分解”策略：**使用一个强大的神经声码器生成高质量的声学载体（carrier）作为最终听觉信号；同时，运行一个修正后的三维声道生物力学模型，生成与声学载体同步、物理上自洽的发音运动（如下颌、唇、舌、软腭等）**。这种设计期望在获得可理解音频的同时，为语音科学家、临床工具、发音训练系统等提供一个可观察、可分析、物理真实的发音状态可视化窗口。

### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：**时长保持的声学载体 + 同步发音运动**。通过一个“对齐”机制，将发音动作序列与声学载体的时间线严格同步，并确保动作的物理合理性。

- **关键技术细节（五阶段流程）**：
    1.  **文本和时长准备**：将输入文本转换为音素序列和时长方案，确定每个单词的持续时间。
    2.  **发音动作生成**：控制器产生一个时间索引的动作序列 `a_{1:T}`，包含下颌、唇、舌、软腭、喉、气流等可解释的参数。公式 (1): `a_{1:T} = π_θ(x, p, d)`.
    3.  **三维声道模型仿真**：动作序列驱动一个修正后的3D声道模型，产生状态轨迹 `s_{t+1}`。公式 (2): `s_{t+1} = F_ϕ(s_t, a_t, c_t)`. 该模型包含皮肤、骨骼、舌头等完整器官和气流标记。
    4.  **声学载体准备**：使用参考/神经TTS或声码器生成一个**时长保持**（确保与视觉动作同步）的声学载体 `y_C`。公式 (3): `y_C = V(x, d)`.
    5.  **JEPA/RL 对齐与优化**：这是核心对齐步骤。
        - **JEPA 模块**：对动作、网格状态、气流、声学特征等进行联合嵌入预测。
            - 编码器 `z_t = E_ω(o_t)` 将观测 `o_t` 映射到潜在空间 `z_t`。
            - 预测器 `ˆz_{t+k} = P_ψ(z_{1:t}, a_{t:t+k})` 从当前潜在上下文中预测未来状态的潜在表示。
            - JEPA 损失 `L_JEPA` (公式 6) 最小化潜在预测值与停止梯度的真实目标值之间的差距。
        - **RL/CEM 轨迹选择**：使用多目标奖励函数 `R` (公式 7) 来评估和选择候选动作轨迹，目标是最大化联合收益。
            - `R = 0.64 R_{ASR} + 0.16 R_{JEPA} + 0.12 R_{UTMOS} + 0.08 R_{timbre}`
            - `R_{ASR}`: ASR 识别结果的准确率。
            - `R_{JEPA}`: JEPA 模块的潜在一致性。
            - `R_{UTMOS}`: 语音质量评分。
            - `R_{timbre}`: 音色保持度。

### 3. 实验设计：数据集与评估方法

- **数据集**：作者构建了一个**诊断性（非大规模语料库）** 测试集。包含 **24 个单词**，组成 **12 个最小对（minimal pair）**。这些最小对全面覆盖了：双唇音、齿龈音、软腭音的**清浊对立**；擦音/唇齿音的**清浊对立**；**鼻音位置**对比；**流音**对比；**高、前、低/后**元音对比；**圆唇化**对比；**滑音**对比。
- **评估基准与方法**：论文**没有与任何现有方法进行定量对比**。评估是“自诊断”式的，主要依靠以下自动指标：
    - **ASR 指标**：精确词恢复（Exact word recovery）、词错误率（WER）、字符错误率（CER）。
    - **语音质量**：UTMOS（自动语音质量评估模型）。
    - **音色一致性**：平均基频（f0）变化、频谱质心变化、以及自定义的 **timbre-guard 得分**。
    - **对齐质量**：JEPA 得分、组合奖励。
    - **主观/定性评估**：通过**录像**（视频 + 音频）让专家审查动作是否可见、是否合理、是否与音频同步。未报道具体主观评测得分。

### 4. 资源与算力

- **文中未明确提及任何算力信息**。没有说明使用了哪种 GPU、多少张卡、训练时长了多久。

### 5. 实验数量与充分性

- **实验数量**：非常有限。仅有 **1 组实验**，是在 **24 个词**的单一测试集上进行的，没有报告在不同数据集、不同模型上的验证。
- **充分性评估**：**严重不充分**。
    - **无消融实验**：没有单独评估JEPA模块、RL/CEM各分量对最终效果的影响。
    - **无对比基线**：没有与任何现有的语音驱动人脸/声道动画方法进行比较。
    - **规模极小**：24个单词的诊断性测试远不足以证明方法的通用性和鲁棒性，无法代表句子级、多说话人、自然对话等复杂场景。
    - **主观评测缺失**：虽然提出了定性评估，但并未报道正式的用户或专家打分结果，所有核心指标均为自动模型估计（UTMOS, ASR）。
    - **公平性存疑**：由于缺乏对比基线，无法客观判断该方法是否优于其他方案。

### 6. 论文的主要结论与发现

- **主要结论**：论文提出的混合系统（声学载体 + 三维发音运动 + JEPA/RL对齐）能够在**有限的最小对测试集**上实现：
    - **良好的音频可懂度**：ASR 词错误率 (WER) 为 8.33%，字符错误率 (CER) 为 4.17%。
    - **合理的音频质量与稳定性**：UTMOS 得分为 3.174，音色保护（timbre-guard）得分高达 0.947。
    - **有效的对齐**：JEPA 平均分 0.864，组合奖励平均分 0.883，表明发音动作与声学载体的时间对齐和潜在一致性较好。
- **系统贡献**：提供了一套能生成同步、可检查、物理合理的三维声道运动的框架和评估协议，为需要可视化发音状态的应用场景（如语音科普、发音训练）提供了可能性。

### 7. 优点：方法或实验设计上的亮点

- **新颖的混合设计（Carrier + Model）**：巧妙地避开了物理合成音频质量不高的痛点，利用神经声码器的优势提供最终音频，同时保留了物理模型的可解释性。这是一个非常实用且有创意的思路。
- **可解释性**：模型输出的不是黑盒参数，而是动作、网格、气流等可解释的物理量，为深入分析发音机制提供了便利。
- **多目标优化策略**：JEPA + RL/CEM 的复合奖励函数设计，综合考虑了内容保真（ASR）、潜在空间一致性（JEPA）、音质（UTMOS）和稳定性（Timbre），目标明确。
- **修正的3D声道模型**：对气道、气流标记等做了手动修正，提高了模型的物理准确性。
- **最小对诊断测试**：设计的最小对集合覆盖了关键的音韵学对比，是一种聚焦的、有深度的评估方式。

### 8. 不足与局限

- **实验规模极不充分**：这是最致命的缺陷。24个单词的诊断性实验结论完全无法外推。缺乏**句子级、语料库级、多说话人、不同语种**的实验，使其方法有效性存疑。
- **缺乏有意义的对比**：没有与任何基线方法（如视觉TTS、EMA/ MRI驱动动画、其他3D声道模型等）进行比较，无法判断其相对优劣。其提出的“好”结果是绝对孤立的。
- **指标依赖自动模型**：ASR（Whisper等）、UTMOS均是自动模型，其评估结果可能无法完全反映真实的人类感知，尤其对于发音动作的合理性，自动模型无法判断。
- **客观与主观评价分离**：提出了主观评价计划，但未报告任何正式结果。论文结论缺少人类反馈这一关键环节。
- **潜在偏差风险**：RL/CEM 的奖励函数中，ASR权重大（0.64），可能存在过度拟合ASR模型的风险，而非真正优化视觉与音频的自然匹配。
- **应用限制**：系统最终交付的仍然是合成载体的音频，而非原始物理模型产生的音频。对于需要完全物理仿真的场景（如精确模拟病理言语），此方法不适用。

（完）
