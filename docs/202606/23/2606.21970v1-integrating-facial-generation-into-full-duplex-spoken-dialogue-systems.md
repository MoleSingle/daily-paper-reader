---
title: Integrating Facial Generation into Full-Duplex Spoken Dialogue Systems
title_zh: 将面部生成集成到全双工口语对话系统中
authors: "Jingjing Jiang, Atsumoto Ohashi, Ryuichiro Higashinaka"
date: 2026-06-20
pdf: "https://arxiv.org/pdf/2606.21970v1"
tags: ["query:talking-head"]
score: 9.0
evidence: 全双工对话中的音频驱动面部运动生成
tldr: 现有全双工语音对话模型仅支持音频，缺乏人类交流中关键的面部表情。本文提出Moshi-Face，通过构建VQ-VAE面部编解码器将3D头部网格编码为离散令牌，并在Moshi模型中扩展Face Transformer模块非自回归生成面部令牌，实现同步的语音与面部生成。实验表明，Moshi-Face在保持低延迟的同时实现了良好的视听对齐，且对话质量不逊于纯音频模型。这是首个联合处理用户音频和面部输入并同时生成语音和面部运动的全双工对话模型。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.21970v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 691, \"height\": 399, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.21970v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1746, \"height\": 535, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.21970v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 710, \"height\": 219, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.21970v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1729, \"height\": 510, \"label\": \"Table\"}]"
motivation: 全双工语音对话模型缺少面部表达，而面部表情对人类交流不可或缺。
method: 使用VQ-VAE将3D面部网格压缩为离散令牌，并扩展Moshi模型加入非自回归Face Transformer生成面部令牌。
result: Moshi-Face在低延迟下实现音频与面部同步，对话质量与纯音频模型相当。
conclusion: 首个同时处理音视频输入并生成语音与面部动作的全双工对话模型。
---

## 摘要
全双工口语对话模型（如Moshi）能够实现自然、低延迟的语音对话。然而，它们仍仅限于音频模态，缺乏人类交流中不可或缺的面部表情。我们提出了Moshi-Face，这是第一个全双工对话模型，它同时处理用户的音频和面部输入，并同时生成语音和面部运动。我们首先构建了一个矢量量化变分自编码器（VQ-VAE）作为面部编解码器，将从面部视频中提取的3D头部网格编码为紧凑的离散令牌（称为面部令牌），并反之从这些令牌重建3D网格。然后，我们通过一个面部变换器模块扩展了Moshi，该模块非自回归地生成面部令牌，使Moshi-Face能够实时生成同步的音频和面部令牌。实验表明，Moshi-Face在低延迟下实现了视听对齐，同时保留了原始纯音频模型的对话质量。

## Abstract
Full-duplex spoken dialogue models, such as Moshi, enable natural, low-latency voice conversations. However, they remain limited to the audio modality, lacking the facial expressions that are integral to human communication. We present Moshi-Face, the first full-duplex dialogue model that jointly processes the user's audio and facial input while simultaneously generating speech and facial motion. We first construct a vector-quantized variational autoencoder (VQ-VAE) as a face codec that encodes 3D head meshes extracted from facial videos into compact discrete tokens, referred to as face tokens, and conversely reconstructs 3D meshes from these tokens. We then extend Moshi with a Face Transformer module that generates face tokens non-autoregressively, enabling Moshi-Face to produce synchronized audio and face tokens in real time. Experiments show that Moshi-Face achieves audiovisual alignment at low latency while preserving the dialogue quality of the original audio-only model.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

**研究动机**：  
- 人类对话本质上是实时的、双向的、多模态的，包含语音、面部表情、头部运动、手势等非语言线索。  
- 现有全双工语音对话模型（如 Moshi）虽然实现了低延迟的实时双向语音交互，但仅局限于音频模态，无法处理或生成面部行为（如唇动、表情、头部运动），这与面对面交流的需求不符。  
- 已有的多模态对话系统虽然能理解和生成面部行为，但基于轮换式（turn-based）架构，无法支持同时说话、实时反馈等自然动态，限制了交互的自然性。

**整体含义**：  
论文旨在构建首个能够同时处理用户音频和面部输入、并同步生成系统语音和面部运动的全双工多模态对话模型，使对话代理具备与人类相似的同时性多模态交互能力。

## 2. 论文提出的方法论：核心思想、关键技术细节

**核心思想**：  
- 以 Moshi 全双工语音对话模型为基础，扩展其架构以支持实时的面部令牌生成。  
- 利用 VQ-VAE 将 3D 面部运动编码为离散令牌（face tokens），并与音频令牌在相同帧率（12.5 Hz）下对齐。  
- 引入非自回归的 Face Transformer 模块，基于对话模型的隐藏状态和音频/文本令牌同步生成面部令牌。

**关键技术细节**：

1. **面部编解码器（Face Codec）**：
   - 架构：VQ-VAE，包含编码器 E、量化器 Q（码本 Z）、解码器 D。
   - 输入：3D 面部顶点序列 X₁:ₜ ∈ ℝᵀ×ⱽ×³，每帧为 FLAME 顶点位移（相对于中性表情）。
   - 编码器：1D 卷积下采样 + Transformer，时间下采样因子 r=2（25 fps → 12.5 Hz），每帧输出 N=8 个潜在向量。
   - 量化：每个潜在向量映射到码本中最近邻条目，得到 N 个离散索引（face tokens）。
   - 解码器：镜像编码器结构，上采样重建运动。
   - 训练损失：Lvq = Lrec + λq Lq + λvel Lvel，包含 L1 重建损失、量化损失、速度损失。

2. **输入输出流**：
   - 在 Moshi 原有文本令牌流（1 条）和音频令牌流（每用户 M=8 条，共 2M 输入、M 输出）基础上，新增面部令牌流（每用户 N=8 条，共 2N 输入、N 输出）。
   - 所有令牌均以 12.5 Hz 帧率处理，音频/面部令牌相对文本令牌延迟一个时间步以增强稳定性。

3. **Moshi-Face 架构**：
   - **RQ-Transformer**：7B 参数的自回归解码器，生成隐藏状态 hᵢ₊₁ 及文本/音频令牌。
   - **Face Transformer**：非因果的非自回归 Transformer，在每个时间步并行生成 N 个面部令牌。
     - 构建统一条件向量 e_allᵢ₊₁ = hᵢ₊₁ + 文本嵌入 + 各音频令牌嵌入之和。
     - 通过投影和可学习位置编码得到 N 个查询向量 qₙ，经非因果自注意力后由线性头预测面部令牌。
     - 训练时采用教师强制（输入前一时刻真实面部令牌），损失为交叉熵 Lface，总损失 LMoshi-Face = Ltext + Laudio + λ Lface。

## 3. 实验设计：数据集、基准、对比方法

**数据集**：
- 使用 Meta 的 Seamless Interaction Dataset 的子集，约 180 小时对话数据（约 3,400 段对话），包含时间对齐的语音转录、分离声道音频、单人视频。
- 使用 VHAP 管线从单目视频提取 3D 面部网格（FLAME 拓扑，5,143 个顶点，25 fps）。
- 文本和音频使用 Moshi tokenizer；面部运动通过训练的 Face Codec 编码为令牌。

**基准（Baselines）**：
- **Moshi**：原始预训练纯音频全双工模型。
- **Moshi-ft**：Moshi 在 180 小时 Seamless 数据上微调（不含面部令牌）。
- **Reconstructed face**：使用真实音频令牌 + 真实面部令牌解码，作为视听同步上限。
- **Random face**：随机采样面部令牌，作为下限。

**对比方法**：
- Moshi-Face 三种消融变体：
  - 无 Face Transformer 预训练（跳过Step 1）
  - 无全模型微调（跳过Step 2，仅训练 Face Transformer）
  - 无 t-1 时刻面部令牌输入

**评估指标**：
- 视听同步：LSE-D（Lip Sync Error Distance，越低越好）、LSE-C（Lip Sync Error Confidence，越高越好），基于 SyncNet。
- 语音自然度：UTMOS（预测 MOS 分数）。
- 语义质量：LLM-as-a-Judge（使用 GPT-5-mini 评估连贯性、自然度、相关性、总体质量，1-5 分）。

**实验设置**：
- Teacher-forced：提供真实音频令牌，单独评估面部生成能力。
- Free-run：两个相同 Moshi-Face 模型互相交互，模拟真实全双工对话。

## 4. 资源与算力

论文中明确说明了计算资源：
- 使用了名古屋大学超算中心“Flow”的算力（未具体说明 GPU 型号、数量、训练耗时）。
- 面部编解码器在 70 小时数据上训练 70 个 epoch，batch size=4。
- Moshi-Face 训练：Step 1 采用 500 训练步，batch size=32；Step 2 采用 1,200 训练步，batch size=16。具体 GPU 型号和数量未提及。

## 5. 实验数量与充分性

**实验组数**：
- 面部编解码器：比较了 4 种码本大小与嵌入维度组合（128/256 × 64/128）。
- Moshi-Face 对比了 4 个 baseline 和 3 个消融变体，在 teacher-forced 和 free-run 两种设置下评估。
- 消融实验包括：有无预训练、有无全微调、有无前一步面部令牌输入。
- 所有 LLM-as-a-Judge 评估进行了 3 次平均。

**充分性评估**：
- 实验覆盖了视听同步和对话质量的主客观指标，对比了上下限，消融实验设计合理。
- 但缺乏真实人类用户的主观评测（论文在结尾也提到未来工作计划进行人类评估），目前仅用自动指标和 LLM 作为评委，可能存在偏差。
- 训练数据来源单一（Seamless Interaction 数据集），未见跨数据集泛化实验。

## 6. 论文的主要结论与发现

1. **视听同步**：Moshi-Face 在 teacher-forced 下 LSE-D=8.76（接近上限 8.53），在 free-run 下仍保持同步，表明能有效对齐语音和面部运动。
2. **对话质量**：Moshi-Face 的 UTMOS 低于原始 Moshi（微调导致领域泛化下降），但在 LLM-as-a-Judge 评估中连贯性最高，自然度第二，总体质量与 Moshi 相当。说明引入面部令牌未损害对话质量，甚至可能提供有益的多模态上下文。
3. **消融结果**：
   - Face Transformer 预训练（Step 1）对同步至关重要。
   - 全模型微调（Step 2）不可或缺，否则同步和语义质量均显著下降。
   - 跳过 t-1 面部令牌输入会在 free-run 下改善 LSE-D 但降低 LSE-C 和 UTMOS，存在鲁棒性与质量之间的折中。
4. 面部编解码器：较大码本（K=256）和嵌入维度（C=128）可获得更低的重建误差和更高的码本利用率。

## 7. 优点：方法或实验设计上的亮点

- **创新性**：首个将全双工语音模型扩展为同时处理/生成音频和面部运动的系统，填补了多模态全双工对话的空白。
- **技术设计**：
  - 使用 VQ-VAE 将 3D 面部运动离散化为同步帧率的令牌，与 Moshi 的音频令牌格式统一，便于联合建模。
  - 非自回归 Face Transformer 设计，利用面部令牌内部的独立性实现并行生成，降低延迟。
- **数据构建**：从大规模对话视频中提取 3D 面部网格，构建了 180 小时的 3D 视听对话数据集（公开来源 Seamless Interaction），解决了现有数据集过小的问题。
- **实验严谨**：设置了 teacher-forced 和 free-run 两种评估，包含上下限基准，并进行了三项消融分析，验证各组件的贡献。

## 8. 不足与局限

- **实验覆盖不足**：
  - 缺乏真实人类主观评价（仅靠自动指标和 LLM 评委，可能存在偏差）。
  - 未评估不同语言、不同文化背景下的泛化能力。
  - 未测试在噪声环境或低质量视频输入下的鲁棒性。
- **技术局限**：
  - 面部编解码器是非因果的（需要完整输入序列），目前无法支持真正的流式输入；论文提及未来将替换为因果编解码器。
  - 3D 面部运动从视频中提取，精度受限于 VHAP 管线，且重建后的唇动可能与真实视频仍有差距。
  - 训练数据仅来自单一数据集 Seamless Interaction，可能引入领域偏差。
- **资源问题**：未详细报告计算资源（GPU 型号、总训练时长），不利于复现和评估成本。
- **局限性说明**：论文本身在“局限”部分只提到未来计划，未充分讨论当前工作的限制。

（完）
