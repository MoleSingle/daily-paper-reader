---
title: "Wan-Streamer v0.1: End-to-end Real-time Interactive Foundation Models"
title_zh: Wan-Streamer v0.1：端到端实时交互基础模型
authors: "Lianghua Huang, Zhifan Wu, Wei Wang, Yupeng Shi, Mengyang Feng, Junjie He, Chenwei Xie, Yu Liu, Jingren Zhou, Ang Wang, Bang Zhang, Baole Ai, Chen Liang, Cheng Yu, Chongyang Zhong, Jinwei Qi, Kai Zhu, Pandeng Li, Peng Zhang, Wenyuan Zhang, Xinhua Cheng, Yitong Huang, Yun Zheng, Zoubin Bi"
date: 2026-06-23
pdf: "https://arxiv.org/pdf/2606.25041v1"
tags: ["query:talking-head"]
score: 10.0
evidence: 端到端音频驱动动画和视频生成，直接支持说话头部合成
tldr: 现有级联交互系统依赖独立模块，存在延迟高和误差累积问题。本文提出Wan-Streamer，一个原生流式、端到端的视听交互基础模型，将语言、音频、视频统一建模于单个Transformer，使用块因果注意力与因果编解码器实现增量流式。模型端延迟约200ms，总交互延迟约550ms，支持160ms流式单元。该工作首次验证了端到端流式模型在实时全双工视听交互中的有效性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.25041v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1700, \"height\": 799, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.25041v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1708, \"height\": 458, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.25041v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1725, \"height\": 897, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.25041v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1728, \"height\": 1201, \"label\": \"Table\"}]"
motivation: 消除级联系统中VAD、ASR、TTS等独立模块带来的延迟累积与错误传播，实现低延迟、全双工的自然视听交互。
method: 设计块因果注意力与因果编解码器，将视觉、音频、文本输入与输出交错排列，在单一Transformer中联合学习感知、推理与生成。
result: 模型端响应延迟约200ms，结合350ms双向网络延迟后总交互延迟约550ms，流式单元粒度达160ms（25fps）。
conclusion: Wan-Streamer证明了端到端流式多模态模型在实时交互场景下的可行性，为低延迟视听通信提供了统一方案。
---

## 摘要
我们提出了Wan-Streamer，一个原生流式、端到端的交互基础模型，从头设计用于实时、低延迟、全双工的音视频交互。Wan-Streamer在单个Transformer中无缝地将语言、音频和视频同时作为输入和输出建模，其中序列表示为交错的视觉、音频和文本输入令牌以及视觉、音频和文本输出令牌，通过块因果注意力进行增量流式协调。与依赖独立VAD、ASR、语言、TTS、音频驱动动画或视频生成模块的级联交互系统不同，Wan-Streamer不依赖外部语言、语音、虚拟形象或视频生成模块：感知、推理、生成、响应时序、话轮管理以及跨模态同步都在一个统一模型中联合学习，减少了流水线延迟和错误累积。为了支持自然的音视频响应性，我们围绕流式能力重新设计了整个架构，包括因果编码器、因果解码器、块因果注意力以及低延迟多模态令牌调度，使得在25 fps下流式单元可短至160毫秒。当结合350毫秒的双向网络延迟时，Wan-Streamer实现了约200毫秒的模型侧响应延迟和约550毫秒的总交互延迟，支持亚秒级双工音视频通信。这些结果使Wan-Streamer成为面向低延迟流式交互的统一、端到端、多模态交互基础模型。

## Abstract
We present Wan-Streamer, a native-streaming, end-to-end interactive foundation model designed from the ground up for real-time, low-latency, full-duplex audio-visual interaction. Wan-Streamer seamlessly models language, audio, and video as both input and output within a single Transformer, where the sequence is represented as interleaved visual, audio, and text input tokens together with visual, audio, and text output tokens, coordinated by block-causal attention for incremental streaming. Unlike cascaded interactive systems that rely on separate VAD, ASR, language, TTS, audio-driven animation, or video-generation modules, Wan-Streamer does not rely on external language, speech, avatar, or video-generation modules: perception, reasoning, generation, response timing, turn management, and cross-modal synchronization are learned jointly within one unified model, reducing pipeline latency and error accumulation. To support natural audio-visual responsiveness, we redesign the entire stack around streamability, including causal encoders, causal decoders, block-causal attention, and low-latency multimodal token scheduling, enabling streaming units as short as 160 ms at 25 fps. Wan-Streamer achieves approximately 200 ms model-side response latency and approximately 550 ms total interaction latency when combined with 350 ms bidirectional network latency, supporting sub-second duplex audio-visual communication. These results position Wan-Streamer as a unified, end-to-end, multimodal interactive foundation model for low-latency streaming interaction.

---

## 论文详细总结（自动生成）

# Wan-Streamer v0.1 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **研究动机**：人类交互本质上是流式、全双工的（边看边听边说边反应），而现有的人工智能系统大多采用级联架构，依赖独立的 VAD、ASR、LLM、TTS、音频驱动动画或视频生成模块。这类流水线在模块边界产生等待时间，累积识别与同步误差，且难以联合学习响应时序、话轮管理、身份保持等行为。
- **整体含义**：实时视听交互不应只是多模态理解与多模态生成的简单组合，而需要一种原生流式、端到端的统一模型，将语言、音频、视频同时作为输入和输出，在单个变换器内联合处理，以实现亚秒级全双工通信。
- **背景**：已有语音对话模型（如 Moshi、Seeduplex）实现了全双工语音交互，但不生成视觉代理；交互式数字人（如 MIDAS、X-Streamer）依赖独立模块组装；端到端视听代理研究尚处于早期（如 Body of Her）。Wan-Streamer 试图填补这一空白。

## 2. 方法论：核心思想、关键技术细节

### 核心思想
- 用一个单一的 Transformer 将语言、音频、视频统一建模为交错的输入/输出因果序列，通过块因果注意力实现增量流式生成。感知、推理、生成、响应时序、话轮管理、跨模态同步全部联合学习，无需外部模块。

### 关键技术细节
- **数据表示**：用户观测 \(u_k = (u^t_k, u^a_k, u^v_k)\) 和代理响应 \(y_k = (y^t_k, y^a_k, y^v_k)\) 以流式单元（160 ms，25 fps）交错排列，作为因果上下文。
- **因果架构**：
  - **因果 VAE**：严格因果的音频和视频变分自编码器，支持流式潜在编码。
  - **因果编码器/解码器**：因果音频和视频编码器与解码器。
  - **块因果注意力**：在 Transformer 内实现增量流式处理。
- **生成建模**：
  - 语言响应：离散令牌，交叉熵损失。
  - 音频/视频响应：连续潜在空间，使用条件流匹配。对于当前单元，噪声潜在变量 \(z^m_\tau = (1-\tau)z^m_0 + \tau\epsilon^m\)，模型估计速度场 \(\frac{\partial z^m_\tau}{\partial \tau}\)，损失函数为 \(L^m_{FM} = E_{\epsilon^m} \| f_\theta(z^a_\tau, z^v_\tau, c_k, \tau) - \frac{\partial z^m_\tau}{\partial \tau} \|_2^2\)。
- **训练三阶段**：
  1. **独立任务预训练**：基于语言模型初始化，混合理解任务（图像/音频/视频理解、ASR、TTS、文本对话）和生成任务（图像/音频/视频生成），对齐模态。
  2. **端到端交互训练**：使用全双工交互数据（用户输入和代理输出交错），学习响应时序、中断处理等。
  3. **蒸馏用于低延迟推理**：使用更强的教师模型（CFG、更多求解步）蒸馏到学生模型，并采用滚动蒸馏（self-forcing + 分布匹配）缓解长程退化。
- **推理管线——思考者-执行者（Thinker-Performer）**：
  - 思考者：负责因果编码器、短令牌因果 Transformer（语言预测与状态更新）、因果解码器。
  - 执行者：仅负责流匹配求解器（生成音频/视频潜在变量）。
  - 两者通过 KV 缓存和潜在变量交换实现流水线重叠：当前帧感知、上一帧解码、下一帧潜在去噪、通信在相邻流式单元间并行，模型侧延迟约 200 ms。

## 3. 实验设计：数据集、基准、对比方法

- **数据集**：论文未公开具体数据集名称或规模，仅描述了一个广泛的训练数据混合物：
  - 理解类：图像/音频/视频理解、文本对话、ASR、TTS、音频对话等。
  - 生成类：图像/音频/视频生成、联合音视频生成。
  - 端到端交互数据：文本、音频、视频在输入和输出侧同时出现。
- **基准与对比方法**：
  - **表1**：与实时语音/全模态对话系统比较响应延迟，包括 Doubao Realtime Voice、Seeduplex、GPT-4o Realtime API、Hume EVI 3、Gemini Live API、Sesame、Moshi、Qwen3/3.5-Omni、MiniCPM-o 4.5。主要指标为“用户可见的响应延迟”（含网络或模型侧）。
  - **表2**：与视觉代理、流式虚拟形象和音视频生成系统比较运行时间指标（FPS、首帧延迟、chunk延迟等），包括 Body of Her、MIDAS、U-Mind、X-Streamer、LPM 1.0、MAViD、M.I.O、VASA-1、TalkingMachines、StreamAvatar、AvatarForcing、LiveTalk、Hallo-Live、OmniForcing 等。注意大部分对比系统报告的是组件级指标而非端到端延迟。
- **评估场景**：Wan-Streamer 在 192p 分辨率下验证了端到端流式设计，延迟测量采用 160 ms 流式单元，模型侧 200 ms，总交互延迟约 550 ms（含 350 ms 双向网络延迟）。同时提供了关于自然度（空闲/倾听状态持续行为生成）和中断/主动发言能力的定性描述。

## 4. 资源与算力

- 论文未明确给出完整的训练算力信息（GPU 型号、数量、训练时长等）。
- 推理阶段描述为“two-GPU thinker-performer serving path”，即部署时使用两块 GPU（可能为 A100 或其他），但未说明具体型号和内存。
- 训练规模：模型初始化来自“语言模型”（可能基于 Qwen2.5 或 Qwen3 系列），三阶段训练可能使用大规模 GPU 集群，但正文未提及具体数量。元数据仅提到“初步的 192p 输出分辨率验证”，高分辨率后续扩展。

## 5. 实验数量与充分性

- **实验数量**：主要实验集中在延迟对比（表1和表2），以及定性的自然度、中断和主动发言行为描述。未包含定量用户研究（MOS）、FID、FVD 等生成质量指标，也没有提供详细的消融研究（如不同注意力机制、损失权重、蒸馏效果等）。
- **充分性与公平性**：
  - 延迟对比谨慎地按测量边界分类（模型侧、第一包、API 等），避免直接比较不同口径的数据，较公平。
  - 但其他系统多为语音或部分模态，Wan-Streamer 涵盖视听全双工，横向对比存在差异。表2中 WAN-Streamer 是唯一在“全循环交互系统”中报告绝对响应延迟的，但其他系统可能未公开此指标。
  - 缺乏训练数据规模、模型参数量、下游任务量化评估，实验置信度有限。论文自述为“v0.1”和“proof of concept”，承认不完整。
- **结论**：实验设计在延迟比较上较为全面，但缺乏生成质量和任务性能的定量验证，充分性不足。

## 6. 主要结论与发现

- Wan-Streamer 证明了原生流式、端到端的全双工视听交互模型在技术上的可行性。
- 通过完全因果的架构和思考者-执行者推理管线，模型侧响应延迟可低至约 200 ms，总交互延迟约 550 ms，支持亚秒级通信。
- 端到端设计消除了模块边界延迟和误差累积，并自然实现了连续的倾听行为、中断处理和主动发言。
- 目前以 192p 分辨率作为概念验证，高分辨率扩展是后续工作。

## 7. 优点：方法或实验设计亮点

- **全面因果设计**：从编码器、注意力、解码器到 VAE 全部强制因果，将流式能力作为模型约束而非后处理，理论优雅且一致。
- **统一模态表示**：语言、音频、视频在输入和输出侧以交错的令牌序列进入同一 Transformer，真正的“全模态”建模。
- **思考者-执行者流水线**：巧妙地将模型拆分为两个组件，通过 KV 缓存交换保持统一状态，同时实现理解与生成的并行，大幅降低感知延迟。
- **滚动蒸馏**：针对流式模型训练-测试不匹配问题提出解决方案，提升长程生成稳定性。
- **延迟比较的边界清晰**：论文明确指出模型侧延迟、总交互延迟、网络延迟的关系，并与公开系统保持测量边界透明，体现严谨性。

## 8. 不足与局限

- **分辨率与视觉质量有限**：当前仅验证 192p 输出，远低于实际应用需求。生成质量（MOS、FVD、唇音同步精度等）未量化评估，仅凭定性描述不足以证明视觉自然度。
- **缺少定量消融**：未对因果 VAE、块因果注意力、三阶段训练、蒸馏策略等关键组件进行消融实验，难以判断各部分贡献。
- **数据集不公开**：训练数据的具体组成、规模和分布未披露，影响可复现性和泛化分析。
- **对比公平性受限**：虽然延迟边界透明，但与其他系统在模态覆盖、硬件配置、推理策略上存在根本差异，直接对比仍有偏差。
- **资源需求不透明**：训练所需的 GPU 数量、时长未公开，难以评估成本和可扩展性。
- **实验数量不足**：仅聚焦延迟，未评估对话质量、指令遵循、长会话一致性、跨语言支持等维度。用户研究缺失。
- **应用限制**：实时性依赖于网络延迟假设（350 ms 双向），在较差网络条件下可能退化。当前为 v0.1 版本，商业化成熟度低。

（完）
